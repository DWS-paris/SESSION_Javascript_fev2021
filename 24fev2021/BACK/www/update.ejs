<!-- Charger le header -->
<%- include('statics/header') -%>

<main>
    <% if( type === "page" ) { %>
        <%- include('components/form-page', { action: 'PUT' }) -%>
    <% } %>

    <% if( type === "portfolio" ) { %>
        <%- include('components/form-portfolio', { action: 'PUT' }) -%>
    <% } %>

    <% if( type === "contact" ) { %>
        <%- include('components/form-contacts', { action: 'PUT' }) -%>
    <% } %>

    <% if( type === "experience" ) { %>
        <%- include('components/form-experience', { action: 'PUT' }) -%>
    <% } %>
</main>

<!-- Charger le footer -->
<%- include('statics/footer') -%>